# ========================================
# DATABASE CONFIGURATION
# ========================================

# MySQL Database (Primary)
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_this_password
DB_NAME=resana

# MongoDB (User Authentication)
MONGODB_URI=mongodb://mongo:27017/vtu_results

# ========================================
# APPLICATION CONFIGURATION
# ========================================

# Node.js Backend
NODE_ENV=production
PORT=3000

# JWT Authentication
JWT_SECRET=change_this_to_random_32_char_string
JWT_EXPIRES_IN=7d

# Python Scraper Service URL
SCRAPER_SERVICE_URL=http://backend:8000

# ========================================
# FRONTEND CONFIGURATION
# ========================================

# API Base URL (for frontend)
VITE_API_URL=http://localhost:3000

# ========================================
# OPTIONAL: TESSERACT OCR PATH
# ========================================

# For Windows (outside Docker)
# TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe

# For Docker (auto-installed)
# TESSERACT_CMD=/usr/bin/tesseract

# ========================================
# NOTES:
# ========================================
# 1. Copy this file to .env and fill in actual values
# 2. NEVER commit .env to Git (contains sensitive data)
# 3. For Docker: DB_HOST should be service name (mysql, mongo)
# 4. For local dev: DB_HOST should be localhost or 127.0.0.1
# 5. Generate strong passwords for production!
